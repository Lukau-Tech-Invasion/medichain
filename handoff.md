# MediChain open-work register

**Updated:** 2026-09-20

This is the active register for the broad "implement every unfinished feature"
work.  It does not turn historical audit claims into present facts: each entry
below was re-checked against the current source before being recorded.

## Corrected on review, 2026-09-19

The 2026-09-18 worktree compiled and its focused tests passed, but five static
gates failed, three patient-app tests failed, and a review of the diff found
defects no gate measures. Each is fixed and covered by a test:

| Defect | Fix |
| --- | --- |
| `POST /api/national-id/verify` was unauthenticated (allowlisted as "stores nothing") yet now wrote to the admin identity-review queue | Requires registration staff (`require_clinical_staff`); removed from the public allowlist in `check-endpoint-auth.py`; anonymous-caller test |
| Consent page mapped access grants/requests as snake_case; the server sends camelCase (`rename_all = "camelCase"`), so every grant rendered blank | Shared types now camelCase and used directly; the page test fixture now carries the server's shape, including a pending request |
| Note templates defined twice; the render copy had drifted (`\\n`) and rendered literal backslash-n; renderer was recursive (NASA rule 1) | One `builtin_note_templates()` table; flat, single-pass placeholder substitution that never re-substitutes an entered value |
| H&P addendum allowed any role that can *read* records; read-append-write lost concurrent addenda; draft edit restamped the author and could turn a record signed in between back into a draft | `can_edit_medical_records` for addenda; author-only drafts that keep their author; `HistoryPhysicalRepository::update_if_unchanged` (compare-and-set on `updated_at`, both backends, PostgreSQL test); bounded retry for addenda |
| Progress notes filed "Full code", hospital day 1 and problem status "stable" on every note (pre-existing, newly visible to the gate) | `CreateProgressNoteRequest` (rule 11) with those fields optional; the page sends only what it collected |
| Progress-note *draft* schema refused any draft with an empty section (pre-existing: `partial()` still rejects `''`) | Draft schema asks only for the patient and bounds lengths |
| Patient search matched identifiers with `ILIKE '%q%'`, including the national-ID *digest* and wallet, so "Ed" or "Abebe" returned unrelated patients in the picker; memory and PostgreSQL matched differently (pre-existing, newly reachable from the picker) | `repositories::patient_search::PatientSearchCriteria`: exact identifier match (national ID via its keyed hash) or all name tokens, one definition for both backends |
| Patients registered before the blind index could not be found by name | `patient_name_index::backfill_missing_name_index`, run once at startup: bounded, idempotent, does not touch `updated_at`, logs counts only (ADR-0009 amended) |
| Name-search and national-ID hash tests mutated process-global key env vars — a race with every concurrent search/registration test | Pure `*_with_key` cores; no test writes those variables |
| Gates: `retract_symptom` missing from the audit vocabulary (PostgreSQL rejects the audit row after the retraction commits); 3 missing i18n keys; an over-length download handler; FHIR ingest role undecided | Migration `20260919000001`; keys added; structured-document dispatch extracted; FHIR ingest accepted with registration's roles (owner decision 2026-09-19) |

## Implemented or source-verified in the current worktree

| Area | Status | Evidence |
| --- | --- | --- |
| Patient name search | implemented, verified | Blind-index migration, exact-identifier/name-token matching shared by both backends, and a startup backfill for pre-index rows. Unit tests cover identifier fragments, whole identifiers, names, inactive rows, backfill, roster order and undecryptable rows. Not yet exercised against a live server's picker. |
| FHIR Patient transaction ingest | implemented, source-verified | `POST /api/fhir/r4/Bundle` accepts the declared bounded Patient transaction. |
| Clinical document access in My Records | implemented, source-verified | Structured-document downloads enforce patient-scoped authorization before rendering. |
| Incomplete language choices | implemented, source-verified | Only the fully translated locale is active; incomplete locales are not offered as complete interfaces. |
| Fabricated patient fallbacks | implemented, source-verified | The affected patient pages use empty/error states rather than clinical sample values. |
| Standalone GCS, lab result entry, consent withdrawal, MFA enrolment, guardianship and appointment check-in | implemented, source-verified | The corresponding pages import and call their shared-client APIs. |
| Laceration repair filing | implemented, source-verified | The doctor portal sends `POST /api/clinical/laceration`, refreshes through the list endpoint, and the server validates required measurements. |
| Insurance-card image persistence | implemented, source-verified | Front and back images now persist encrypted content plus metadata hashes separately and have an owner/provider-authorised decrypt/read path. |
| Offline-cache accuracy and category clearing | implemented, verified | IndexedDB failures now render an explicit unknown state rather than sample clinical records; category clearing deletes the persisted matching entries. Patient offline-sync UI test passes. |
| Telehealth recording control | implemented, verified | Only the assigned provider may start or stop their session recording; unsupported actions are rejected. Seven focused authorization/action tests pass. |
| Physician-order in-progress metric | implemented, verified | The UI now counts its normalized `active` state rather than the pre-normalized API value; the realistic shared-client fixture and focused page test pass. |
| Guardianship history | implemented, verified | The patient family page now separately shows caller-scoped current, expired, and revoked guardianships without presenting ended authority as usable record access. Focused page tests pass (6/6) and the patient typecheck passes. |
| Nursing care-plan templates and persisted-list contract | implemented, verified | Selecting a template now opens an editable draft with real goal/intervention entries; submitted plans persist those structured entries instead of empty arrays. The list now maps the server’s snake-case repository records and nested JSON items without fabricating demographics. Focused page tests pass (5/5). |
| History and physical templates | implemented, verified | Selecting a template opens the editable H&P form with the matching examination type selected and its patient-information section available. The focused page test passes (4/4). |
| History and physical amendment workflow | implemented, verified | The API supports author-only draft update and appends attributable amendments to signed H&Ps without rewriting their signed document body; both writes are compare-and-set. Handler tests cover author ownership, signed immutability, the read-only-role refusal and successive addenda; a PostgreSQL test covers the stale-copy refusal. |
| AMA legal acknowledgment payload | implemented, verified | The portal now sends the server contract's snake-case risk disclosures, capacity determination/basis, refusal context, acknowledgment, and witness fields instead of UI-only camelCase values. Doctor portal typecheck passes; the focused page runner hung before producing results and remains unverified. |
| MAR barcode verification | partially implemented, verified | Replaced the unsafe client substring check with a persisted server barcode scan and fail-closed handling. Medication-barcode success remains unavailable until the backend has a real medication barcode registry; the UI no longer treats a string containing `MED` or `PAT` as verified. Doctor portal typecheck passes. |
| Intake/output trend actions | implemented, verified | The visible trend controls now export the displayed table as escaped CSV and invoke browser printing, with accessible labels. The focused page test passes (4/4). |
| Note templates | implemented, verified 2026-09-19 | Facility-wide clinician templates (owner decision): doctors and nurses create them, every clinician sees and uses them, the author or an administrator deactivates one (a conditional write; the row is kept, ADR-0005). Built-ins are read-only, served as an ordered table (the built-in SOAP had been served Assessment, Objective, Plan, Subjective). `rendered_sections` preserves order. The page saves on the server and reports success only after it confirms. Also fixed: the page was in the admin navigation only, which the router enforces, while the API served only doctors and nurses — nobody could use it; its buttons failed WCAG contrast (3.68:1) once it could render cards. Driven in the browser: create, reload, survive an API restart, use, deactivate. |
| Lab-result visible actions | implemented, verified | The selected result can now be printed or exported as escaped CSV containing the displayed tests and values. Focused page tests pass (3/3). |
| Imaging orders | implemented, verified | Imaging order entry uses the typed authenticated client for the durable surgical-radiology endpoint and reloads the server list after creation rather than inserting a browser-only row. Read failures are visible rather than rendered as an empty worklist. Doctor typecheck and focused page tests (3/3) pass. |
| Pathology report editing and finalization | implemented, verified | The pathology worklist now saves preliminary/final report content through a dedicated authenticated update endpoint and reloads from durable storage. The server stamps the authenticated clinician, records the actual audit role, requires diagnosis plus microscopy before finalization, and rejects overwriting a final report. API compile, focused Rust tests (2/2), doctor typecheck, and focused page tests (4/4) pass. The pre-existing PostgreSQL restart test still needs a separately terminating database-backed run. |
| eMAR administration history | implemented, verified | Dose administrations are now read from the daily MAR records the nursing write endpoint updates, with patient/date metadata added by the bounded staff-only history endpoint. The portal reloads that durable stream after a dose is recorded rather than manufacturing a browser-only history row. API compile and focused portal tests (4/4) pass. |
| Care-plan register and blood-bank refresh | implemented, verified | The generic care-plan screen now reads the authenticated emergency care-plan register rather than an obsolete nursing route, rendering the repository's ISO timestamps safely. Blood-product orders and transfusion documentation reload the signed server register after save rather than retaining provisional browser records. Focused portal tests (5/5) and doctor typecheck pass. |
| Barcode scan history and camera capture | implemented, verified | The scanner maps its persisted scan schema (`scan_id`, `barcode_value`, entity resolution and timestamp) rather than inventing a successful local record. Camera scans now use the browser Barcode Detection API; unsupported browsers and no-detection conditions are explicit failures, and neither failed calls nor undetected values appear in durable-looking history. A successful scan re-reads the caller-scoped persisted history. Focused portal tests (3/3) and doctor typecheck pass. Patient/medication/equipment barcode registries remain unavailable, so unresolved values are explicitly warnings rather than identity claims. |
| Emergency-access expiry display | implemented, verified | The emergency screen counts down from the server-issued grant expiry retained in the patient store rather than reconstructing a 15-minute browser timer. It refreshes live, clears through the store when the grant expires, and its record action navigates to the granted patient record. Focused portal tests (3/3) and doctor typecheck pass. |
| Nursing Hub repository contracts | implemented, verified | The hub maps MAR, I&O and care-plan repository entities rather than treating them as an obsolete page-specific payload. It displays only persisted medication administrations; an order with no administered dose remains unacted and links to eMAR rather than receiving an invented schedule. Focused portal tests (4/4) and doctor typecheck pass. |
| Medication data fallbacks | implemented, verified | The doctor eMAR and patient medication screen no longer insert clinical demo prescriptions when the real API returns no medication data, including under demo configuration. Incomplete persisted prescriptions also cannot acquire invented directions or an `active` status: the patient UI marks the absent regimen/instructions as not recorded. Empty records now remain empty; demo scenarios must be seeded through the database. Focused doctor eMAR tests (3/3), patient medication tests (5/5), and both portal typechecks pass. |
| Dashboard acknowledgement controls | implemented, verified | Nurse, laboratory and pharmacy dashboards no longer present an acknowledgement button that only wrote to the browser console. Their distinct critical-value, vital-sign and interaction worklists remain the action destinations, preventing a non-persistent click from being read as a clinical acknowledgement. Focused dashboard tests (18/18) and doctor typecheck pass. |
| Lab QC calibration records | implemented, verified | Calibration no longer announces a browser-only success. The portal persists a separate calibration record stream, lists it through the audited clinical registry, and refreshes only after acceptance. The server assigns operator/time/ID, validates type/result/lot/expiry, and refuses expired calibrators. API compile, focused Rust handler tests (2/2), doctor typecheck, and focused portal tests (3/3) pass. |
| MFA step-up retry | implemented, source-verified | `useStepUp` recognizes the server's structured MFA refusals, retains the denied role/guardian mutation, uses the enrolled TOTP challenge to install the elevated token, and retries it. `UserManagementPage` and `PatientDetailPage` render the dialog; focused hook tests cover retry, invalid codes, enrollment-required and assurance lookup. Live MFA enrollment/browser-wallet proof remains external verification, not missing application wiring. |
| Patient language and regional formats | implemented, verified | The language screen now persists the authenticated user's full BCP 47 locale through the language-preference API and merges its validated regional-format document into the durable user settings record. It reloads only renderable saved values, preserves unrelated preferences (such as wearables), and reports any load/save failure instead of claiming success. Patient typecheck and the focused page suite (6/6) pass. |
| Emergency-card/profile absent-value handling | implemented, verified | The emergency card and patient profile no longer convert missing organ-donation/DNR fields into negative clinical assertions, and the card no longer stamps a missing server update time as the current browser time. They explicitly render those values as not recorded. Focused emergency-card tests (7/7), profile tests (4/4), and patient typecheck pass. |
| Break-glass emergency-card absent-value handling | implemented, verified | The clinician card shown during emergency access no longer converts absent directives into “Full Code” or “Not a Registered Donor,” does not invent an update time, and patient-search records no longer seed those unknown values as `false`. Focused component test (1/1), emergency-access/search tests (6/6), and doctor typecheck pass. |
| Clinician patient-summary export | implemented, verified | The formerly inert Export control now downloads an explicitly scoped JSON copy of the authorised patient summary currently displayed; it includes only loaded server fields and an export timestamp, without representing itself as a complete longitudinal record. Focused patient-detail tests (10/10, including direct download URL lifecycle coverage) and doctor typecheck pass. |
| Clinician patient-detail editing | implemented, verified | The formerly inert Edit control now opens a provider-only clinical editor for allergies, medications, chronic conditions and organ-donor status. It uses the existing authorized update API, updates the displayed record only after a successful response, and keeps DNR changes out of this generic editor because the server requires a separately attested advance-directive workflow. Focused patient-detail and nurse-dashboard tests (13/13) plus doctor typecheck pass. |
| Specimen collection worklist contract | implemented, verified | `GET /api/clinical/specimens` now returns typed persisted collection rows in its canonical `specimens` envelope and fails visibly when its repository is unavailable instead of claiming an empty queue. The portal consumes that envelope, labels the returned patient ID as an ID rather than fabricating a patient name/MRN, and recognises a collection row as already collected rather than exposing an inert “Mark collected” action. API regression test, focused portal tests (4/4), doctor typecheck and i18n gate pass. |
| Patient medical-record download | implemented, verified | The documents tab’s Download action now calls the existing owner-authorized, durably audited document-download API before creating a browser download. It shows an in-progress state, surfaces a failed download, and sanitizes the returned filename. Focused patient medical-history and lab-trends tests (7/7), patient typecheck and i18n gate pass. |
| Patient medication reminders | implemented, verified | Patients can create reminders through the typed authenticated API, then deactivate their own active reminder and see the refreshed server list. The deactivation guard now resolves the caller’s linked patient record rather than comparing a wallet address to a `PAT-` identifier. Focused patient workflow tests (4/4), patient typecheck and API compile pass. |
| Patient symptom report | implemented, verified | The symptom tracker’s formerly inert report action now downloads a CSV from the already-loaded, server-recorded diary. CSV cells are escaped and patient-entered formula-like values are neutralized before download; the report is a patient-controlled copy, not a claimed provider share. Focused unit coverage validates the export shape and formula protection. |
| Symptom-checker care follow-up | implemented, source-verified | The result screen’s Schedule and Find Care actions now lead to the existing patient appointment route, whose booking panel loads the real provider directory and availability before submitting a proposal. They no longer consume a tap without a destination. |
| Wearable registration type contract | implemented, verified | Patient-facing labels such as `apple-watch` and `fitbit` are now separated from the API’s clinical device categories (`smartwatch`, `fitness_band`, etc.), so registrations no longer degrade to the server’s `Other` type. The device display also derives from persisted manufacturer rather than guessing every smartwatch is Apple. Focused portal coverage validates the registered payload. |

## Still missing — implementation requires a product/security decision

| Area | Current evidence | What must be decided before implementation |
| --- | --- | --- |
| Per-record crypto-shredding | `encryption_keyring` remains deployment-key encryption; `key_envelopes`/`organization_keys` are metadata foundations only. There is no KMS/HSM or private-key unwrap boundary. | Select and provision the KMS/HSM/key-custody model, recipient-envelope algorithm, legacy-record migration, key-destruction authorization and audit/restore procedure. A server-held substitute would not provide the promised patient-level irrecoverability. |
| WebAuthn/passkey biometric login | The patient setting is visibly disabled and there is no WebAuthn credential-registration/assertion protocol or durable credential store. | Approve the credential model, relying-party ID/origins, attestation policy, recovery/step-up rules and storage schema. This needs a vetted WebAuthn dependency or a reviewed protocol implementation. |
| Research/secondary-use preference enforcement | The preference is persisted in `/api/settings`; no research-export path currently exists to consult it. | Define the permitted export/secondary-use workflow, legal basis, de-identification standard, reviewer authorization and audit retention. Do not add an unscoped export endpoint just to consume a toggle. |
| Blood-unit inventory | The clinical registry explicitly reports that inventory tracking is not implemented; existing blood-bank records are type/screen and transfusion documentation, not stock control. | Define inventory ownership, unit/barcode provenance, reservation, compatibility, expiry, recall and reconciliation workflow before adding a clinical inventory ledger. |
| Insurance-card versus payer-policy verification | Patient-entered cards use the JSON card store; provider verification reads the separate payer-policy repository. The patient UI no longer marks a card verified after a failed request. | Choose one authoritative insurance model or a controlled link between card and payer-policy records, then make verification update that model with payer evidence and audit history. |
| Telehealth recording retention | The retention component is implemented but no recording artifact pipeline registers a sensitive artifact. | Approve recording custody, participant consent, E2EE/provider configuration, BAA/DPA, upload/storage, legal-hold and deletion verification before wiring a recorder. |
| Order sets: create, duplicate, delete | `OrderSetsPage` has the defect note templates had: all three change only the browser's list and announce success; the API has only `GET /api/order-sets`. The page is also admin-navigation only. | An order set bundles medication and investigation orders, so a shared one with a wrong dose reaches every patient it is applied to. Decide who may author and who must approve a shared set (e.g. pharmacist sign-off), whether sets are versioned, and whether retiring one affects orders already placed from it. |
| CDS rule editor | `CDSAlertsPage` creates, duplicates, and deletes browser-only configuration-shaped objects, but the available CDS API persists patient-specific fired alerts. The real tunable model is facility threshold overrides, not arbitrary UI conditions/actions. | Define the clinical-governance workflow: facility identity, rule/threshold authorisation, evidence review and approval, versioning, activation/rollback, and whether custom rules may block care. Mapping the current form to a patient alert would manufacture the wrong clinical record. |
| Patient prescription refill requests | The patient medication page showed an inert “Request Refill” action whenever a prescription reported one or fewer refills. There is no refill-request endpoint, authorized recipient or prescribing/pharmacy decision workflow, so the control is now an explicit unavailable message rather than a false success path. | Define refill-request ownership, prescription eligibility/controlled-medication policy, requested quantity, prescriber/pharmacy routing, approval/denial lifecycle, audit retention and patient notification before adding a mutation endpoint. |
| Patient claim EOB documents | The claims API returns claim status and financial fields, but no owner-authorized EOB artifact or URL; the client maps EOB as absent. The unreachable EOB download button was removed rather than left as a fake capability. | Define EOB source/custody, payer authorization, encrypted artifact storage or signed-download lifetime, patient/provider access rules, retention and audit evidence before adding a document endpoint. |
| Secure-message attachments | Patient messaging persists text-only messages; it has no attachment DTO, encrypted object-store artifact, recipient authorization, malware/content validation, download audit or retention policy. The inert attach button is now absent and the composer says attachments are unavailable. | Define permitted attachment types/size, encryption and key access, clinical-record linkage, scanning/quarantine, patient/provider authorization, audit events, retention/hold and download semantics before implementing uploads. |

## Externally blocked or owner-operated work

| Area | Blocker | Required evidence to close |
| --- | --- | --- |
| South Africa/Nigeria live national-ID verification | DHA/NIMC provider contract and sandbox/production credentials are not present. | Authorised provider integration exercise: success, denial, timeout, malformed response and credential failure; production must remain fail-closed. |
| Real transcription/translation of patient material | No approved BAA/DPA and no server-side recording artifact suitable for a transcription provider. | Approved provider agreement, consent proof, secure artifact pipeline, and integration failure/reconciliation tests. |
| Blockchain operational claims | No multi-validator testnet, consortium governance, validator hosts, session-key procedure or recovery rehearsal. | Governed testnet, finality/partition/upgrade/recovery drills and operational ownership. |
| Legal Terms, Privacy/PAIA and data-subject contact path | Legal content is not supplied by engineering. | Reviewed, approved legal copy and a maintained support/contact process. |

## Verified against a live server, 2026-09-19

All against a locally built API (commit `588bc50`) on PostgreSQL, not the
Docker image:

| Suite | Result |
| --- | --- |
| Live probes of every changed endpoint, with the pages' payloads | 22/22 |
| Doctor-portal browser suite (`playwright.local-api.config.ts`) | 78/78 |
| Patient-app browser suite (`playwright.local-api.config.ts`) | 73/74 — see below |
| Role journeys (`scripts/role-journeys.ts`) | 266/266, 2 legitimate skips |
| Cross-role qualification | 117/117 |
| Synthetic e2e on a freshly created database | 261/0 |

The startup name-index backfill ran against the dev database: 152 patients
indexed, 69 undecryptable (rows sealed under a key this deployment no longer
holds — they are equally absent from the roster).

**Open: browser verification for WCAG 2.4.11.** `accessibility.spec.ts` "a
focused control is never hidden behind the sticky header" (mobile) failed once
in the full run and once in twelve isolated repeats: the dashboard's
notification and sign-out buttons ended up under the shared layout's sticky
nav. `index.css` now reserves 8rem for the persistent nav plus possible banner
for document scrolling and `:focus-visible` targets. The source mitigation
still needs the browser suite rerun; it is not evidence of a closed defect.

## Closed 2026-09-22 — the last two fake saves, and the bell that led nowhere

Each verified against the running Docker stack after rebuilding the `api` and
`nginx` images, with the page's own payload, read back through the endpoint the
next person in the workflow uses.

| Area | Status | Evidence |
| --- | --- | --- |
| Order sets | implemented, verified live | A doctor drafts; a pharmacist approves or rejects (owner decision 2026-09-22). Was `setOrderSets([...])` plus a success toast, over `GET /api/order-sets` serving three hardcoded bundles in a vocabulary the page could not read — so the built-ins rendered nameless with empty order lists. Now: `POST /api/clinical/order-sets` (doctor only), `POST .../approval` (pharmacist only, conditional on the draft state, self-approval refused), `POST .../deactivate`; built-ins and stored sets served in one shape. Only an approved set is orderable. The screen was in the admin navigation alone — which the router enforces — so neither the drafting doctor nor the approving pharmacist could open it; both navigations now carry it. 6 handler tests; 9/9 live checks. |
| CDS rules | implemented, verified live | Administrators only (owner decision 2026-09-22). Create, Duplicate, Enable/Disable and Delete all changed the browser's array and announced success; the list came from `/api/platform/list/cds-alerts`, which is the alerts that *fired*, so the screen showed instances under the heading "rules"; the on/off switch posted to `/api/cds/alerts/{id}/respond`, which records a clinician's response to an alert and never changed a rule. Now: `POST/GET /api/admin/cds/rules`, `POST .../enablement`, `POST .../retire`, both conditional writes. A new rule is disabled and in test mode. Clinical staff may read the rules that govern them; only an administrator writes one. 4 handler tests; 6/6 live checks. |
| Notifications | implemented, verified live | The header bell navigated to `/notifications`, which matched no route in the doctor portal, so React Router's catch-all bounced every click to the dashboard: a badge with a count nobody could open. `unread_count` was `notifications.len()`, so the count could never be cleared, and two entries were stamped `Utc::now()` rather than with the time of the thing they were about. Now: a real screen, a durable per-user read marker (`POST /api/notifications/read`), entries timed by their own record. The patient app's page filtered on `is_read`/`read`, fields the endpoint has never sent, and now uses the same marker. 2 handler tests, 3 page tests; 3/3 live checks. |
| Forced sign-out every 30 seconds | fixed, verified | `DashboardPage` polled `/api/dashboard/doctor` with a hand-rolled `fetch` that carried only the legacy `X-User-Id` headers, and on a 401 called `restoreSession()` — which fails closed **by design** — and then `logout()`. One 401 on that panel returned a clinician to the login screen from any page. Two browser tests had been reporting it as "never reached /orders" and "never reached /splint". Now reads through the shared client, which refreshes once; only a 401 that survives that ends the session. |
| Browser harness fixture path | fixed | `support.ts` resolved `.browser-test/fixtures.json` as `resolve(cwd, '..', ...)`, which only works when the suite is launched from inside `client/doctor-portal`. Run as documented, 39 of 83 tests failed with a bare ENOENT naming a `client/.browser-test` that has never existed — a message that reads as "nobody seeded the fixtures". Now walks up from the working directory. |

## Verification still required

- The browser suites above ran against the local debug build; rebuild the
  Docker images (`api` and `nginx`) before trusting the stack on :80.
- Re-run the Substrate pallet test suite on a host able to build the required
  Polkadot SDK workspace.
- Qualify all configured external adapters with real authorised credentials;
  disabled-by-default code is not production proof.
