target/
client/node_modules/
client/*/node_modules/
client/*/dist/
client/playwright-report/
client/test-results/
.git/
.idea/
.vscode/
.agents/
.ai/
.aiassistant/
.claude/
.junie/
docs/
tests/
# The blockchain workspace (pallets, runtime, node) is a separate cargo
# workspace on a different Polkadot SDK release. No image here builds it, and
# shipping it as build context costs time for nothing. Replaces the stale
# `node/` and `runtime/` entries, which moved under blockchain/.
blockchain/
mobile-examples/
medichain/
**/*.log
**/.DS_Store
